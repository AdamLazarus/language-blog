<div class="container">
  <div class="row">
    <div class="col-md-8">
        <h2><%= @user.username %></h2>
        <% if current_user.id == @user.id %>
        <%= link_to "Edit Account", edit_user_registration_path %><br/>
        <% end %>
        <%= link_to "View Posts", user_posts_path(@user) %><br/>
        <%= link_to "View Corrections", user_corrections_path(@user) %>
      </div>
    </div>
  </div>
